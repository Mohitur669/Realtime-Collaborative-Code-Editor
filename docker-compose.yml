version: "3"

services:
  code-editor:
    build:
      context: .
      dockerfile: ./Dockerfile
    image: "<your_docker_username>/code-editor"
    ports:
      - "5000:5000"
      - "3000:3000"
      - "8000:8000"